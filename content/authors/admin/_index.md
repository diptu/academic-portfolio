---
# Profile data for this author lives in `data/authors/admin.yaml` (referenced as `username: admin`
# in content pages). To publish a standalone author profile page at /authors/admin/, remove the
# `build` and `cascade` settings below.
build:
  render: never
cascade:
  build:
    render: never
    list: always
---
